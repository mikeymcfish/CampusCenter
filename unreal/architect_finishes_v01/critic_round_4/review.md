# Superseded capture — round 4 fallback regression

The builder identified dropped finish geometry in these captures before the critic issued a score. The non-Nanite fallback was not rebuilt with the full mesh in the commandlet. This set is retained as diagnostic evidence and is not the final review.

The full editor rebuilt the ray-tracing fallback at 100% triangles. The critic's replacement round 4 capture set is in `../critic_round_4_final/`.
